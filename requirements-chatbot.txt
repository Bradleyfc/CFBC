# Dependencias específicas del chatbot con post-instalación automática
# Usar: pip install -r requirements-chatbot.txt

# Dependencias de IA/ML para el chatbot
numpy>=2.3.5
sentence-transformers>=5.1.2
faiss-cpu>=1.13.1
transformers>=4.57.3
torch>=2.9.1
hypothesis>=6.148.7
hf_xet>=1.2.0

# Post-instalación automática (ejecutar después de instalar las dependencias)
# NOTA: Ejecutar manualmente después de pip install:
# python chatbot/install_hooks.py