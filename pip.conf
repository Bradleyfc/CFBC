# Configuración pip para el proyecto CFBC
# Este archivo puede copiarse a ~/.pip/pip.conf para configuración global

[global]
# Configuración general
timeout = 60
retries = 3

[install]
# Hooks post-instalación
# Nota: pip no soporta hooks automáticos, usar scripts alternativos